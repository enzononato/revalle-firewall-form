{
  "name": "revalle-firewall-form",
  "version": "1.0.0",
  "description": "Formulario publico para solicitacao de desbloqueio de sites no firewall da Revalle",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^2.0.0",
    "nodemailer": "^8.0.7",
    "pg": "^8.13.1"
  }
}
